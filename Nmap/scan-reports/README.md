# Nmap Scan Reports

This directory contains sanitized Nmap scan reports from authorized local lab exercises.

## Report

- `localhost-service-scan.txt` — Nmap service/version detection scan against the local host.

## Finding

The local host was reachable, but no open TCP services were detected among the default 1000 ports scanned.

> All scans are performed only against systems where I have permission to test.


